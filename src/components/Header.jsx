// Import di router dom per i link
import { Link } from "react-router-dom"

const Header = () => {
    return (
        <nav>
            <div>
               <Link className="" to="/">Bool Movie</Link>
            </div>
        </nav>
    )
}

export default Header